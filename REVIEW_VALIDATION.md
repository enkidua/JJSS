# JJSS 전체 코드베이스 검증 리뷰

이 문서는 JJSS 저장소의 **전체 파일을 빠짐없이 검토**하기 위한 리뷰 기록 양식입니다. 직업재활계획서 기능만 확인하는 문서가 아니라, 애플리케이션 코드·설정·데이터 저장·Electron 패키징·정적 자산·스크립트·문서까지 포함합니다.

리뷰어는 각 파일을 실제로 열어 확인한 뒤 파일별 상태를 기록합니다. 오류나 명확한 개선 필요가 발견되면 이 문서의 이슈 목록에 재현 절차와 파일 위치를 남깁니다. 검토 중 임의로 코드를 고치지 말고, 수정이 필요하면 별도 작업으로 분리합니다.

## 1. 리뷰 범위와 판정 기준

### 포함 범위

- React 화면, 공통 컴포넌트, 라우팅, 상태 관리
- IndexedDB/localStorage 저장소, 암호화, 백업·복원
- Gemini/AI, OCR, 매칭, 문서 재생성 서비스
- 직업재활계획서 매핑·미리보기·로컬 PDF/PNG/DOCX 출력
- TypeScript/Vite/Tailwind/Electron 설정과 배포 패키징
- public 이미지·템플릿 및 루트 스크립트
- README, 테스트 체크리스트, 복구 문서, 개선 제안 문서

### 상태 값

| 상태 | 의미 |
|---|---|
| `PASS` | 파일을 읽고 역할·의존성·오류 가능성을 확인했으며 즉시 수정할 문제가 없음 |
| `PASS-WARN` | 현재 동작은 수용 가능하지만 추적이 필요한 경고·기술 부채가 있음 |
| `FAIL` | 재현 가능한 오류, 데이터 손실 위험, 보안 문제 또는 빌드 차단 문제가 있음 |
| `N/A` | 해당 파일을 이번 환경에서 실행할 수 없거나 검토 대상이 아님. 사유 필수 |
| `TODO` | 아직 검토하지 않음 |

`FAIL`은 “예쁘지 않음”이나 장기 개선 제안이 아니라, 현재 기능·데이터·빌드·보안에 명확한 문제가 있는 경우에만 사용합니다.

## 2. 리뷰 메타데이터

| 항목 | 기록 |
|---|---|
| 리뷰어 | Codex |
| 리뷰 시작/종료 | 2026-08-14 / 2026-08-17 |
| OS / Node / npm | Windows / Node v24.19.0 / npm 11.17.0 |
| 브라우저 또는 Electron 버전 | Chromium 기반 사전 검증 / Electron v36.9.5 프로세스 스모크 |
| 대상 브랜치·커밋·작업 트리 상태 | `feature-reconnect-updated-functions-20260508` / `6972614` / 기존 변경이 많은 dirty tree |
| API 호출 검증 여부 | ☐ 호출함 ☑ 저장 결과로 대체 ☑ 외부 API 호출 불가 |
| 최종 판정 | ☐ 통과 ☑ 조건부 통과 ☐ 실패 |

### 개인정보 주의

- 실제 이용자 이름, 주민등록번호, 주소, 연락처, 장애 관련 민감정보를 입력하지 않습니다.
- 아래 검증용 값만 사용합니다: `검증용이용자`, `1996.01.02`, `검증용 주소`, `010-XXXX-XXXX`.
- 스크린샷·콘솔·다운로드 파일명에 API 키나 실제 개인정보가 없는지 확인합니다.

## 3. 전체 파일 인벤토리 리뷰

각 행의 `읽기`, `의존성`, `오류/개선` 확인란을 완료한 뒤 상태와 근거를 기록합니다. 파일이 다른 파일을 동적으로 읽거나 생성하면 그 관계도 메모에 남깁니다.

### 3.1 루트·빌드·배포 파일

| 파일 | 확인 포인트 | 읽기 | 의존성 | 오류/개선 | 상태 | 근거·메모 |
|---|---|---:|---:|---:|---|---|
| `package.json` | scripts, 의존성 버전, Electron builder 대상, 시작점 | ☑ | ☑ | ☑ | PASS-WARN | 현재 version 3.0.0, appId 유지, 기존 설치와 같은 per-machine, 데이터 보존, signed build 사전 검사 연결 확인. 실제 업그레이드는 수동 검증 필요. |
| `package-lock.json` | package.json과 lock 일치, 플랫폼별 optional dependency | ☑ | ☑ | ☑ | PASS | root version 3.0.0과 `html2canvas@1.4.1` lock 반영 확인. |
| `vite.config.ts` | React 플러그인, alias, base, 빌드 출력·외부화 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `tsconfig.json` | 프로젝트 참조 및 공통 컴파일 정책 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `tsconfig.app.json` | strict, module resolution, include 범위 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `tsconfig.node.json` | Vite 설정의 타입 검사 범위 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `tailwind.config.js` | content 경로, 테마, 누락 클래스 가능성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `postcss.config.js` | Tailwind/autoprefixer 연결 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `index.html` | root 요소, 제목, favicon, 정적 경로 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `electron/main.cjs` | BrowserWindow 보안, 파일 경로, 외부 링크, 종료 처리 | ☑ | ☑ | ☑ | PASS-WARN | HTTP/HTTPS 허용 목록 연결 및 Node 문법 검사 완료. Electron 런타임 클릭 검증 필요. |
| `electron/externalUrl.cjs` | 외부 URL 파싱·프로토콜 허용 목록 | ☑ | ☑ | ☑ | PASS | HTTP/HTTPS 허용, file/javascript/data/잘못된 URL 차단 집중 테스트 통과. |
| `electron/fileService.cjs` | Documents 경로·native save·가져오기·IPC 검증 | ☑ | ☑ | ☑ | PASS-WARN | category/파일명/확장자/크기/sender 검증과 copy-hash-delete 합성 테스트 PASS. 실제 save dialog 버튼은 수동 검증 필요. |
| `electron/preload.cjs` | contextBridge 최소 API·raw IPC/Node 객체 노출 여부 | ☑ | ☑ | ☑ | PASS | `jjssFiles`의 제한된 invoke 함수만 노출하고 raw ipcRenderer/fs/path/shell을 노출하지 않음. |
| `electron/jjss-installer.nsh` | 실행 프로세스 판정·강제 종료·업그레이드 흐름 | ☑ | ☑ | ☑ | PASS-WARN | `${APP_EXECUTABLE_FILENAME}` 한 개만 exact 검사하고 taskkill 없음. NSIS 컴파일 통과, 실제 업그레이드는 사용자 확인 대기. |

### 3.2 루트 스크립트·검증 산출물

| 파일 | 확인 포인트 | 읽기 | 의존성 | 오류/개선 | 상태 | 근거·메모 |
|---|---|---:|---:|---:|---|---|
| `copy_assets.js` | 복사 대상·덮어쓰기·경로 안전성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `fix_utils.js` | 일회성 수정 범위·재실행 안전성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `fix_image_ocr.sh` | 셸 호환성·경로·삭제/덮어쓰기 위험 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `test_response.ts` | 테스트 목적·민감정보·실행 가능성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `test_image_gen.mjs` | 외부 API 호출·키 노출·생성 파일 관리 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `test_generate.mjs` | 생성 테스트의 재현성·오류 처리 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `test_gen.mjs` | 생성 테스트의 입력·출력·민감정보 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `scripts/check-win-signing.cjs` | 환경변수·로컬 PFX 존재 검사, 비밀값 로그 금지 | ☑ | ☑ | ☑ | PASS | 현재 PFX 파일 존재 확인. 환경변수가 없으면 Vite/패키징 전에 실패하고 비밀번호·인증서 내용을 출력하지 않음. |
| `scripts/check-release-readiness.cjs` | 버전·appId·데이터 보존·민감파일·생성물 변경 사전 검사 | ☑ | ☑ | ☑ | PASS | 버전/appId/productName/보존 설정과 민감파일을 확인하며 현재 `release/` Git 변경은 정리됨. |
| `scripts/test-rehab-plan-mapper.mjs` | 합성 fixture·핵심 필드 기대값·실패 exit code | ☑ | ☑ | ☑ | PASS | 제목형·개조식·긴 문장형 합성 fixture가 `PASS 3/3`. 실제 개인정보·외부 API 미사용. |
| `scripts/test-jjss-file-service.cjs` | 합성 가져오기·비대상 보호·충돌·검증 후 삭제 | ☑ | ☑ | ☑ | PASS | JJSS 7/7, 일반 파일 2/2 보호, `(1)` 충돌, SHA-256 검증 후 이동, 경로·확장자 검증 PASS. |
| `CHATGPT_DIFF.patch` | 현재 소스와 일치하는지, 미적용/중복 변경 여부 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `README.md` | 실행법·환경변수·기능 설명의 최신성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `TEST_CHECKLIST.md` | 기능 목록·검증 절차·현재 구현과의 일치 | ☑ | ☑ | ☑ | PASS | 백업·모델·출력·예산·Electron·로그 점검 항목을 실제 코드 기준으로 추가. |
| `RECOVERY_NOTES.md` | 복구 절차·데이터 손실 주의·실제 코드와의 일치 | ☑ | ☑ | ☑ | PASS | 실제 반영된 최소 안정화와 보존 범위만 기록. |
| `REVIEW_VALIDATION.md` | 본 리뷰 문서 자체의 누락·오류 | ☑ | ☑ | ☑ | PASS | 실행 결과와 수동 검증 한계를 구분해 갱신. |

### 3.3 문서 파일

| 파일 | 확인 포인트 | 읽기 | 의존성 | 오류/개선 | 상태 | 근거·메모 |
|---|---|---:|---:|---:|---|---|
| `docs/JJSS_2_2_RECOVERY_AND_STABILIZATION_PLAN.md` | 복구 범위·완료 표시·현행 구조와의 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/google-workspace-auto-install.md` | 외부 서비스·권한·설치 절차의 안전성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/WINDOWS_INTERNAL_SIGNING.md` | 내부 PFX 보관·환경변수·검증·CER 배포 주의 | ☑ | ☑ | ☑ | PASS | 실제 `%USERPROFILE%\Documents\JJSS-CodeSigning.pfx` 경로, PFX 외부 보관, SecureString 입력, Authenticode 확인과 SmartScreen 한계를 기록. |
| `docs/WINDOWS_RELEASE_CHECKLIST.md` | 빌드 전·업그레이드·데이터 유지·서명 검증 절차 | ☑ | ☑ | ☑ | PASS | 실제 설치 버전을 추측하지 않고 레지스트리 수동 확인, 합성 데이터, 기존 앱 제거 없는 업그레이드와 Authenticode 검증을 기록. |
| `docs/2026-08-12-future-improvement-proposals.md` | 향후 제안만 포함하고 현재 구현으로 오인되지 않는지 | ☑ | ☑ | ☑ | PASS | 전체 개인정보 암호화 확대와 복원 장애 주입 테스트를 후속 제안으로만 기록. |
| `docs/2026-05-08-vocational-image-output-stabilization.md` | 이미지 출력 복구 내용과 코드 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/2026-05-08-updated-feature-recovery-plan.md` | 업데이트 기능 복구 내용과 코드 일치 | ☑ | ☑ | ☑ | PASS | 백업·Gemini·문서/예산 연결·Electron 안정화 섹션을 실제 코드 기준으로 추가. |
| `docs/2026-05-08-training-route-restore.md` | 훈련 라우트·메뉴·화면 동작과 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/2026-05-08-regeneration-and-matching-cleanup.md` | 재생성·매칭 정리 내용과 서비스 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/2026-05-08-menu-route-cleanup.md` | 메뉴/라우트 목록과 `App.tsx` 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/2026-05-08-matching-opinion-db-persistence.md` | 매칭 의견 저장 설계와 실제 저장소 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/2026-05-08-db-gemini-service-cleanup.md` | DB·Gemini 정리 내용과 구현 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/2026-05-08-db-api-document-stabilization.md` | DB/API/문서 안정화 내용과 코드 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/2026-05-08-budget-training-crisis-cleanup.md` | 예산·훈련·위기 기능 복구와 코드 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `docs/2026-05-08-budget-ocr-restoration.md` | 예산 OCR 복구 내용과 OCR 화면/서비스 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |

### 3.4 public 정적 자산

| 파일 | 확인 포인트 | 읽기 | 의존성 | 오류/개선 | 상태 | 근거·메모 |
|---|---|---:|---:|---:|---|---|
| `public/vite.svg` | 사용 여부·브랜딩 잔존 여부 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T1.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T2.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T3.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T4.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T5.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T6.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T7.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T8.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T9.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/T10.png` | 실제 참조 경로·깨진 이미지·내용 적합성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `public/templates/rehab-plan-template.docx` | 원본 양식 구조·개인정보 제거·placeholder·번들 포함 | ☑ | ☑ | ☑ | PASS | 원본 1표 26행×26열과 병합 구조 유지, 36개 placeholder, 원본 개인정보 0건. 원본 대비 `word/document.xml`만 변경. |

### 3.5 앱 진입점·전역 스타일

| 파일 | 확인 포인트 | 읽기 | 의존성 | 오류/개선 | 상태 | 근거·메모 |
|---|---|---:|---:|---:|---|---|
| `src/main.tsx` | React 마운트, StrictMode, ErrorBoundary, 전역 오류 처리 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/App.tsx` | 모든 라우트, 레이아웃 중첩, 미등록/고아 페이지 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/index.css` | 전역 리셋·테마·반응형·인쇄 스타일·충돌 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/vite-env.d.ts` | Vite 타입 선언·불필요한 전역 선언 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |

### 3.6 공통 컴포넌트

| 파일 | 확인 포인트 | 읽기 | 의존성 | 오류/개선 | 상태 | 근거·메모 |
|---|---|---:|---:|---:|---|---|
| `src/components/Layout.tsx` | Outlet·공통 레이아웃·오류 경계·접근성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/JjssFileSaveNotice.tsx` | 저장 경로 안내·폴더 열기·자동 닫힘 | ☑ | ☑ | ☑ | PASS-WARN | 실제 경로와 허용 category 폴더 열기만 제공. 패키징 UI 클릭은 수동 검증 필요. |
| `src/components/Navbar.tsx` | 메뉴/라우트 일치, 현재 경로, 모바일·키보드 동작 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/Toast.tsx` | 메시지 수명·중복·접근성 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/ErrorBoundary.tsx` | 렌더 오류 격리·복구 버튼·민감정보 노출 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/DashboardView.tsx` | 대시보드 집계·빈 상태·로딩/오류 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/CaseHistoryPanel.tsx` | 사례 이력 조회·정렬·삭제·민감정보 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/DocumentReviewTab.tsx` | 문서 검토 탭·저장·재검토·오류 처리 | ☑ | ☑ | ☑ | PASS-WARN | 브라우저에서 WorkMate의 `작성 내용 점검` 탭을 열어 입력 영역과 실행 버튼 렌더링을 확인. 실제 API 응답은 수동 검증 필요. |
| `src/components/DocumentChatView.tsx` | 문서 질의·프롬프트·출력·API 오류 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/JobSeekerModal.tsx` | 이용자 생성/수정·필수값·취소·중복 저장 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/ExpenseDocument.tsx` | 지출 문서 구조·금액·파일 처리 | ☑ | ☑ | ☑ | PASS-WARN | 빈 새 창이 Electron allowlist에 차단될 수 있어 동일 HTML을 hidden iframe으로 인쇄하도록 최소 변경. 타입·빌드 통과, 패키징 Electron 인쇄 대화상자는 수동 검증 필요. |
| `src/components/MatchingView.tsx` | 매칭 결과·의견 저장·빈 상태·오류 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/MaskingView.tsx` | 마스킹 입력·출력·원본 보호 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/MinutesView.tsx` | 회의록 입력·저장·조회·날짜/참석자 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/OCRView.tsx` | 이미지/문서 OCR·파싱·금액·실패 복구 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/PromoDesignView.tsx` | 홍보물 입력·이미지 생성·다운로드 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/ScheduleDesignView.tsx` | 일정 설계·시간 검증·출력 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/UtilitiesView.tsx` | 보조 도구 목록·라우팅·상태 초기화 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/components/RehabPlanTemplatePreview.tsx` | 양식 필드 편집·목표 행·출력 상태·개인정보 안내 | ☑ | ☑ | ☑ | PASS-WARN | 기존 미리보기 안에 누락 경고와 DOCX 버튼을 연결. 강제 차단·자동 생성 없음. 실제 PDF/DOCX UI 저장은 수동 검증 필요. |
| `src/components/RehabPlanTemplatePreview.css` | 모달·인쇄·반응형·긴 문서 레이아웃 | ☑ | ☑ | ☑ | PASS-WARN | 화면 디자인을 유지하고 출력 확인 레이어 및 짧은/긴 행 page-break 구분만 추가. 실제 프린터 장문 레이아웃은 수동 검증 필요. |

### 3.7 페이지

| 파일 | 확인 포인트 | 읽기 | 의존성 | 오류/개선 | 상태 | 근거·메모 |
|---|---|---:|---:|---:|---|---|
| `src/pages/Home.tsx` | 초기 안내·기능 카드·라우트 링크·빈 데이터 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/pages/UserManagement.tsx` | 이용자 CRUD·검색·선택·WorkMate 이동 상태 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/pages/VocationalEvaluation.tsx` | 평가 입력·계산·저장·결과 조회 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/pages/WorkTraining.tsx` | 훈련 계획·진행·상태 복원·저장 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/pages/WorkMate.tsx` | 상담/회의/계획 파이프라인·저장·재생성·미리보기 | ☑ | ☑ | ☑ | PASS-WARN | 브라우저에서 합성 이용자를 선택해 계획 단계와 `작성 내용 점검` 탭 연결을 확인. API 호출·PDF/PNG 출력은 수동 검증 필요. |
| `src/pages/BudgetManagement.tsx` | 사업/예산·지출·OCR·집계·내보내기 | ☑ | ☑ | ☑ | PASS-WARN | 연결 지출 항목 삭제 확인창을 실제 화면에서 확인하고, helper 테스트로 지출 보존·항목 링크만 해제·기타 필드 유지·CSV `항목 미지정` 표시를 확인. 확인 승인부터 저장 후 재진입까지는 수동 검증 필요. |
| `src/pages/AITools.tsx` | 도구 선택·위기 안내·교육/평가/생성·실패 처리 | ☑ | ☑ | ☑ | PASS | 브라우저에서 외부 링크 카드를 열어 이력서 링크가 요청 URL과 정확히 일치함을 확인. |
| `src/pages/InfoMate.tsx` | 정보 조회·검색·외부 링크·오류 처리 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/pages/Settings.tsx` | API 설정·암호화 저장·백업/복원·초기화 | ☑ | ☑ | ☑ | PASS | 브라우저에서 정확한 Gemini 모델 3종·기본 Flash-Lite·설명·백업 UI를 확인하고, 격리 하네스에서 선택 보존과 복원 경로를 검증. |
| `src/pages/Community.tsx` | 게시글·댓글·삭제·입력 검증·민감정보 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/pages/CrisisManual.tsx` | 위기 시나리오·안전 문구·출력 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/pages/WelfareLauncher.tsx` | 외부 복지 서비스 실행·미등록 라우트 여부 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |

### 3.8 서비스·데이터·상태

| 파일 | 확인 포인트 | 읽기 | 의존성 | 오류/개선 | 상태 | 근거·메모 |
|---|---|---:|---:|---:|---|---|
| `src/config/localDB.ts` | DB 버전/스토어·CRUD·트랜잭션·암복호화·백업 | ☑ | ☑ | ☑ | PASS | 별도 origin의 실제 IndexedDB 하네스에서 기존 검증과 함께 직업평가 이력 내보내기/복원, 구버전 백업의 현재 이력 보존, 손상 이력 사전 거부, 인증정보 제외/보존을 확인. |
| `src/config/crypto.ts` | 키 생성/보관·암호화 형식·복호화 실패·키 부재 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/store/authStore.ts` | 인증 상태·초기화·권한 경계 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/store/dataStore.ts` | 전역 데이터 로딩·동시성·캐시 무효화 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/store/settingsStore.ts` | 설정 로딩·API 키 처리·기본값·저장 실패 | ☑ | ☑ | ☑ | PASS | 격리 하네스에서 지원 모델 3종, 구형 값 보정, 지원 Preview 모델 직접 선택 보존을 실제 저장/재로드로 확인. |
| `src/services/gemini.ts` | 프롬프트·모델 선택·키 주입·응답 파싱·재시도 | ☑ | ☑ | ☑ | PASS-WARN | 격리 하네스에서 503 정규화 오류를 상위 file wrapper가 그대로 전달하고 정확한 안내·`status: 503`·`reason: model-high-demand`를 유지함을 확인. 실제 Gemini 오류 응답은 수동 검증 필요. |
| `src/services/ocr.ts` | OCR 입력·파일 크기·파싱·수량/단가·오류 | ☑ | ☑ | ☑ | PASS-WARN | 관련 오류 로그에서 키 포함 URL/원본 오류 제거. 실제 Vision 호출은 수동 검증 필요. |
| `src/services/matching.ts` | 매칭 점수·의견·빈 결과·저장 연계 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/services/clientContextService.ts` | 이용자 문맥 조합·필드 누락·민감정보 범위 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/services/documentRegenerationService.ts` | 문서 재생성·원본 보존·실패 시 복구 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/data/crisisScenarios.ts` | 위기 데이터의 완전성·안전 문구·식별정보 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |

### 3.9 타입·유틸리티

| 파일 | 확인 포인트 | 읽기 | 의존성 | 오류/개선 | 상태 | 근거·메모 |
|---|---|---:|---:|---:|---|---|
| `src/types/rehabPlan.ts` | 계획서 모델·날짜·목표 상태·선택 필드 타입 | ☑ | ☑ | ☑ | PASS | 미리보기와 매퍼의 출력 전용 타입 연결 확인. |
| `src/types/jjssFiles.ts` | preload 저장·경로·가져오기 전역 타입 | ☑ | ☑ | ☑ | PASS | category와 payload/결과 타입을 제한하고 `window.jjssFiles`를 선택 전역으로 선언. |
| `src/types/caseDocument.ts` | 사례 문서 구조와 저장 모델 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/types/budget.ts` | 예산/지출/OCR 타입과 화면 사용 일치 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/utils/budgetItemLinks.ts` | 삭제 항목 연결 조회·항목 미지정 변환·표시명 | ☑ | ☑ | ☑ | PASS | 순수 helper 테스트에서 대상 지출만 선택하고 사업 연결·기타 필드를 유지한 채 항목 링크만 해제하며 CSV 표시가 `항목 미지정`임을 확인. |
| `src/types/matching.ts` | 매칭 결과·의견 타입과 서비스/컴포넌트 일치 | ☑ | ☑ | ☑ | PASS | 생년월일·주소·연락처를 기존 호환 optional 필드로만 추가. |
| `src/types/file-saver.d.ts` | 외부 모듈 선언의 정확성·중복 선언 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/utils/rehabPlanMapper.ts` | 상담/계획 텍스트 매핑·날짜·목표 누락·다국어 라벨 | ☑ | ☑ | ☑ | PASS | 직접 optional 필드 우선순위 유지, Markdown 제목 기호만 최소 정규화하고 합성 fixture `PASS 3/3`. |
| `src/utils/localDocumentExport.ts` | PDF/PNG 출력·파일명·중복 클릭·자원 해제 | ☑ | ☑ | ☑ | PASS-WARN | 기존 PNG 4개 decode 통과. PDF는 긴 행의 전역 break avoid를 해제했으나 실제 프린터 페이지 경계는 수동 검증 필요. |
| `src/utils/jjssFileService.ts` | Electron native 저장·Browser fallback·완료 이벤트 | ☑ | ☑ | ☑ | PASS-WARN | Electron은 제한 API, 브라우저는 기존 anchor fallback을 사용. 실제 native dialog는 수동 검증 필요. |
| `src/utils/rehabPlanDocx.ts` | 로컬 템플릿 치환·XML escape·줄바꿈·실패 처리·파일명 | ☑ | ☑ | ☑ | PASS-WARN | 합성 생성본 구조·한글·줄바꿈·placeholder 제거 통과, Electron `file://` 템플릿 로드 통과. Word 페이지 시각 QA는 환경 제한. |
| `src/utils/rehabPlanReview.ts` | 누락 항목·목표 미확인·비차단 경고 | ☑ | ☑ | ☑ | PASS | 기본정보·계획·사례회의·강점/고려/지원방향·담당자/작성일자를 순수 함수로 점검하며 데이터 변경 없음. |
| `src/utils/docxGenerator.ts` | DOCX 생성 경로·템플릿·사용 여부·실패 처리 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/utils/anonymizer.ts` | 비식별화 범위·원본 보존·정규식 누락 | ☑ | ☑ | ☑ | PASS-WARN | 정적 파일·의존성·오류 경로를 검토했고 타입 검사/프로덕션 빌드에서 차단 오류가 없었음. 해당 기능의 실제 런타임 동작은 별도 수동 검증 필요. |
| `src/utils/fileValidation.ts` | 파일 형식·빈 파일·크기 제한·중복 식별자 | ☑ | ☑ | ☑ | PASS | OCR/AI 문서 20MB, 저장 사진 5MB, 텍스트 10MB 제한과 MIME/확장자 보조 검증을 공통 적용. |
| `src/utils/safeError.ts` | 민감 원문 제외·진단 메타데이터 허용 범위 | ☑ | ☑ | ☑ | PASS | feature/name/code/status/reason 중 영숫자 분류값만 허용하며 message·URL·입력 원문은 제외. |

## 4. 정적 검증 절차

아래 결과를 이 문서의 `정적 검증 결과` 표에 복사합니다. 개발 환경에서 명령이 불가능하면 `N/A` 사유를 기록합니다.

```powershell
# 1) 타입 검사
npx tsc -b --pretty false

# 2) 프로덕션 번들
npm run build

# 3) 패치/공백 오류
git diff --check

# 4) 라우트·내비게이션·스토어·위험 API 후보 검색
rg -n "<Route|to=|navigate\(|indexedDB|localStorage|apiKey|console\.(log|error)|dangerouslySetInnerHTML|innerHTML|TODO|FIXME|any" src electron

# 5) import 경로와 고아 파일 후보 확인
rg -n "from ['\"]|import\(['\"]" src
```

| 검사 | 명령/방법 | 기대 결과 | 실제 결과 | 상태 |
|---|---|---|---|---|
| 타입 검사 | `npx tsc -b --pretty false` | 오류 0건 | `npx.cmd tsc -b --pretty false` 실행, 오류 0건 | PASS |
| Vite 빌드 | `npm run build` | 빌드 성공, 산출물 생성 | `npm.cmd run build` 실행, 1,995 modules·성공. 기존 Browserslist/청크 경고만 있음 | PASS-WARN |
| Git diff 공백 | `git diff --check` | 새 변경의 오류 0건 | 오래된 거대 patch 산출물을 현재 코드 전용 patch로 재생성한 뒤 공백 오류 0건. CRLF 변환 안내만 있음 | PASS-WARN |
| import/라우트 점검 | `rg` 검색 + 파일 대조 | 깨진 import·고아 라우트 없음 | WorkMate의 Preview/DocumentReview import·렌더링과 외부 링크 대조 완료 | PASS |
| 위험 API 점검 | `rg` 검색 + 의도 확인 | 허용된 위치·목적 외 사용 없음 | 외부 URL allowlist 집중 테스트·관련 로그 정적 점검 완료. Electron/API 실응답은 수동 검증 필요 | PASS-WARN |

### 4.1 2026-08-13 실행 검증 증적

- 프로덕션 정적 빌드를 임시 HTTP 서버로 열어 홈 화면과 주요 경로를 실제 DOM으로 확인했다. 검증 종료 시 합성 이용자 `검증용이용자`/`TEST-001` 한 건만 조건부 삭제했고 홈 화면의 등록 이용자 `0명`을 다시 확인했다.
- 설정 화면에는 `gemini-3.1-flash-lite`, `gemini-3-flash-preview`, `gemini-3.1-pro-preview`만 표시되며 기본값과 설명이 정상 렌더링됐다.
- WorkMate에서 `작성 내용 점검` 탭, 예산 화면에서 세부 항목 `메모(선택)`, 업무지원도구에서 `https://service-459909947241.us-west1.run.app/` 링크를 실제 DOM으로 확인했다.
- 별도 origin의 격리 IndexedDB에서 잘못된 배열 형식과 빈 백업을 복원 전에 거부했다. 내보내기 JSON에서 API 키·토큰·비밀번호를 제외하면서 `tokenCount` 같은 일반 필드는 보존했다.
- 백업 복원은 현재 기기의 인증정보를 유지하고 비인증 설정값은 복원했다. quota 오류로 중간 실패를 유발했을 때 이용자 데이터가 복원 전 상태로 되돌아갔다.
- 브라우저 콘솔의 warning/error는 위 검증 동안 0건이었다. 실제 Gemini/Vision API 호출, PDF/PNG 파일 저장, Electron 패키지 설치·제거는 실행하지 않았다.

### 4.2 2026-08-13 최종 안정화 보완 증적

- 격리 하네스에서 503 오류를 `normalizeGeminiError`에 다시 전달해도 같은 오류 객체와 정확한 모델 혼잡 안내가 유지되고, `status: 503`, `reason: model-high-demand`가 보존됨을 확인했다. 400·403·404·429도 각각 요청 형식·권한·모델·quota 사유로 구분됐다.
- 격리 IndexedDB/localStorage에서 직업평가 이력 1건을 전체 백업에 포함해 복원했다. 내보낸 항목에는 허용된 이력 필드만 있었고 주입한 `apiKey` 추가 필드는 제외됐다.
- `vocationalEvaluationHistory`가 없는 구버전 백업은 현재 이력을 유지했고, 배열이 아니거나 항목 구조가 잘못된 백업은 기존 데이터 변경 전에 거부됐다. 현재 기기의 인증정보도 각 복원 시나리오에서 유지됐다.
- 실제 예산 편집 화면에서 연결 지출이 있는 항목 삭제 시 확인창이 발생함을 확인했다. 별도 순수 helper 테스트는 연결 지출 1건만 선택, `projectId` 보존, 항목 링크 해제, 금액·날짜·설명·거래처 보존, CSV `항목 미지정` 표시를 모두 통과했다.
- 지출품의서는 빈 새 창 대신 로컬 hidden iframe에 기존 문서 HTML을 작성해 인쇄하는 구조로 바뀌었고 타입 검사와 Vite 빌드를 통과했다. 패키징된 Windows Electron에서 인쇄 대화상자가 실제로 열리는지는 수동 확인 대상으로 남겼다.
- 실제 Gemini 서비스의 503 응답과 그 뒤 화면 입력/기존 결과 유지, 예산 확인 승인부터 저장 후 재진입까지의 전체 UI 흐름은 사용자 환경 수동 검증이 필요하다.

### 4.3 2026-08-13 Windows 배포·PNG 검증 증적

- `npx tsc -b --pretty false`는 오류 없이 통과했고 `npm run build`는 최신 소스 기준 1,995 modules를 변환해 성공했다. 기존 Browserslist·대형 청크 경고만 있으며 `html2canvas`는 201.88 kB 동적 청크로 분리됐다.
- 환경변수를 제거한 `npm run electron:build:win:signed`는 Vite 실행 전에 의도한 한국어 안내로 중단됐다. PFX 파일은 프로젝트 밖에 존재하지만 현재 셸에 서명 환경변수가 없어 실제 signed build는 `N/A - signing credentials not configured in this shell`이다.
- 최신 소스의 미서명 구조 검증 빌드는 `signAndEditExecutable=false`로 `JJSS Setup 2.3.0.exe`를 생성했고 builder 로그에 `oneClick=false`, `perMachine=true`가 표시됐다. 설치본은 150,182,704 bytes, SHA-256 `3E799B6BBAA1C91FA30686FA9CAE1A6A8E159E1A971927D6E2D3A4F9BEA8536D`, version 2.3.0, Authenticode `NotSigned`였다. 일반 build는 winCodeSign 캐시의 macOS symlink를 만들 Windows 권한이 없어 실패했으므로 구조 검증본을 signed 배포본으로 사용하면 안 된다.
- 기존 설치 제거 키 GUID와 현재 appId 파생 GUID가 모두 `763ac432-8ee1-5a44-b6ab-56ce675b7536`임을 확인했다. 기존 설치와 새 빌드 모두 per-machine이다. 실제 기존 앱 위 업그레이드와 데이터 보존은 자동 실행하지 않았다.
- 기존 1.0 설치 표시명 `JJSS Pro`와 현재 productName `JJSS`는 다르지만, 현재 저장소와 확인된 2.2 설치 파일명 계열을 유지했다. GUID 기반 감지는 일치하며 표시명·바로가기 교체는 실제 업그레이드 수동 확인 대상으로 남겼다.
- 합성 데이터만 사용해 짧은·보통·긴·강제 fallback PNG를 Chromium에서 실제 다운로드했고 각각 PNG 시그니처, 0 byte 아님, 이미지 decode를 통과했다. 테스트 파일과 임시 하네스는 삭제했다.
- PDF 함수, UI/CSS, DB_VERSION/store, userData 경로, API/Gemini 정책은 변경하지 않았다.

### 4.4 2026-08-14 전체 최종 검토 증적

- `src`, `electron`, `scripts`, `docs`, `public` 전체 파일 목록·크기·텍스트 줄 수를 대조하고 모든 인벤토리 TODO를 실제 정적 판정으로 전환했다. T1~T10 PNG는 모두 System.Drawing decode와 0 byte 초과를 확인했고 T1은 시각 확인했다.
- raw 오류 객체를 남기던 전역 처리·ErrorBoundary·dataStore·훈련·예산·매칭·OCR 등 로그를 원문 없는 분류 메타데이터로 제한했다. OpenAI Authorization 헤더의 불필요한 후행 공백을 제거하고 OpenAI/Anthropic 오류 응답 원문을 사용자 메시지로 전달하지 않도록 했다.
- 일괄등록은 전체 행의 열 수와 필수 이름을 저장 전에 검증한다. 형식 오류는 해당 행을 알리고 쓰기를 시작하지 않는다. IndexedDB 자체 실패를 단일 대형 트랜잭션으로 재설계하지는 않았다.
- OCR·직업평가·문서채팅·AI 도구·사업체 사진·훈련 사진·텍스트 파일에서 빈 파일, 지원하지 않는 형식, 과도한 크기와 중복 첨부를 Base64/FileReader 전에 거절하도록 했다.
- 다운로드 Blob URL은 click 직후가 아닌 1.5초 뒤 해제하고, WorkMate 사진 미리보기는 삭제·언마운트 때 해제한다. 매칭 지연 타이머와 Toast 타이머도 언마운트 정리를 추가했다.
- 코드 서명 인증서는 저장소 밖의 사용자 지정 위치를 사용한다. `WIN_CSC_LINK`, `WIN_CSC_KEY_PASSWORD`가 현재 셸에 없어 signed build는 실행하지 않았다.
- 캐시의 Electron 36.9.5 ZIP을 패키지 체크섬과 대조한 뒤 누락된 `node_modules` 바이너리를 복원했다. Electron 앱을 실행해 제목이 있는 응답 가능 창과 4개 프로세스를 확인하고 검증 프로세스를 종료했다. 창 내부 메뉴 순회는 Windows 앱 제어 도구 권한 오류로 수행하지 못했다.

### 4.5 2026-08-14 실무 출력·릴리즈 보강 증적

- 작업 전 `npx tsc -b --pretty false`와 Vite 프로덕션 빌드가 통과했고 `git diff --check` exit code는 0이었다. 작업 후 타입검사와 2,001 modules Vite 빌드도 통과했다. 기존 Browserslist·500kB 초과 청크 경고는 남아 있다.
- `npm run test:rehab-plan-mapper`는 실제 개인정보가 없는 제목형·개조식·긴 문장형 fixture에서 `PASS 3/3`을 출력했다. Markdown 제목 기호를 제거하는 최소 정규화 외 Gemini 프롬프트는 변경하지 않았다.
- 현재 릴리즈 readiness 기준은 package/lock 3.0.0, `com.jjss.desktop`, `JJSS`, `deleteAppDataOnUninstall=false`, signed build 연결, 프로젝트 내부 PFX 없음, Git 추적 민감파일 없음이다. 2.3.0 검증 결과는 과거 기록으로만 유지한다.
- 제공 원본 DOCX의 SHA-256은 `C2DE2518AA11CF69D21EB821E97DEED382117BD52C551135528C596E63B32362`였다. 원본에는 실제 개인정보가 있어 프로젝트에 복사하지 않고 데이터 셀만 36개 placeholder로 바꾼 안전 템플릿을 만들었다.
- 안전 템플릿은 원본과 동일한 1개 섹션·1개 표·26행×26열을 유지하고, ZIP 12개 파트 중 `word/document.xml`만 변경됐다. 원본 이름·전화번호·주민번호 패턴은 0건이었다.
- 합성 데이터 DOCX는 모든 placeholder 치환, 한글, 여러 줄, 표/행/열 유지, 원본 개인정보 미포함을 통과했다. 안전 템플릿 대비 본문 XML만 변경되고 관계·스타일·테마 파트는 유지됐다.
- 빌드된 Electron `file://` 페이지에서 DOCX 템플릿을 fetch와 XHR로 각각 0 byte 초과 읽었다. 외부 서버·Gemini·파일 변환 API는 호출하지 않았다.
- 표준 `render_docx.py`는 LibreOffice 미설치로 실행할 수 없었다. 설치된 Microsoft Word COM 보조 렌더도 120초 시간 초과되어 해당 검증 프로세스만 종료했다. DOCX 페이지별 시각 QA와 실제 PDF 프린터 페이지 경계는 수동 검증으로 남긴다.
- 출력 전 누락 검토는 데이터 자동 생성이나 저장 없이 경고만 표시한다. `계속 출력`과 `돌아가서 수정`을 제공하고 개인정보 공유 경고를 PDF·PNG·DOCX에 공통 적용한다.
- `DB_VERSION` 3, store, migration, 기존 데이터, API 키 암호화, userData, UI 색상, Navbar, 메뉴, 라우트, 기존 textarea, PDF/PNG 알고리즘은 변경하지 않았다.

## 5. 런타임 스모크 테스트

검증용 이용자 한 명만 생성하고 각 단계의 결과를 기록합니다. 모든 주요 화면에서 새로고침, 빈 상태, 오류 상태, 저장 후 재진입을 한 번씩 확인합니다.

### 5.1 진입·공통 UX

| ID | 절차 | 기대 결과 | 상태 | 증적 |
|---|---|---|---|---|
| R-01 | 앱을 처음 실행한다. | 홈 화면이 로드되고 전역 오류 화면이 나타나지 않는다. | PASS | 임시 프로덕션 빌드에서 홈 화면과 등록 이용자 0명 렌더링 확인. 콘솔 warning/error 0건. |
| R-02 | 새로고침/직접 URL 진입을 반복한다. | HashRouter 경로가 유지되고 빈 화면이 없다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-03 | 모든 Navbar 메뉴를 순회한다. | 각 메뉴가 올바른 페이지로 이동하고 현재 메뉴가 표시된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-04 | 브라우저 폭을 데스크톱/태블릿 수준으로 바꾼다. | 가로 넘침·겹침·키보드 포커스 손실이 없다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-05 | 의도적인 렌더 오류 또는 네트워크 오류를 확인한다. | ErrorBoundary/Toast가 사용자에게 안내하고 민감정보를 노출하지 않는다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |

### 5.2 이용자·사례·직업재활 흐름

| ID | 절차 | 기대 결과 | 상태 | 증적 |
|---|---|---|---|---|
| R-06 | 검증용 이용자를 생성·수정·삭제한다. | 필수값 검증, 중복 방지, 취소, 삭제 확인이 정상이다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-07 | 이용자를 선택하고 사례 이력을 확인한다. | 관련 문서/상담/훈련이 올바른 이용자에 연결된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-08 | 평가를 입력·저장·재진입한다. | 계산 결과와 저장 결과가 일치하고 빈/경계값이 안전하다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-09 | 상담·회의·직업재활계획을 생성·수정·저장한다. | 단계 전환, 저장 실패, 재생성 시 원본 보존이 정상이다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-10 | `양식 미리보기`를 연다. | 기본 정보·상담·목표가 자동 매핑되고 누락값은 `추가 입력 필요`로 표시된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-11 | 미리보기에서 값·날짜·목표를 수정한다. | 변경이 즉시 반영되고 목표 최소 1개 제약이 유지된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-12 | PDF/PNG 출력을 각각 실행한다. | 인쇄 대화상자/PNG 다운로드가 열리고 파일명에 개인정보가 없다. | PASS-WARN | 기존 PNG 합성 출력은 통과. PDF 인쇄와 새 누락 경고 전체 UI는 실제 프린터 수동 검증 필요. |
| R-13 | WorkMate를 새로고침한 뒤 다시 연다. | 저장된 단계와 문서가 복원된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-14 | DOCX 출력을 실행한다. | 기관 양식 구조의 로컬 DOCX가 개인정보 없는 기본 파일명으로 저장된다. | PASS-WARN | 합성 데이터 구조 생성과 Electron 템플릿 로드는 통과. 실제 버튼 다운로드와 Word 페이지 시각 QA는 수동 검증 필요. |

### 5.3 훈련·예산·AI·정보 흐름

| ID | 절차 | 기대 결과 | 상태 | 증적 |
|---|---|---|---|---|
| R-14 | 훈련 계획을 만들고 진행 상태를 바꾼다. | 진행률·상태·복원이 일관된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-15 | 예산 사업·지출 문서를 생성한다. | 금액 합계·필수값·수정·삭제가 일관된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-16 | OCR로 검증용 영수증을 처리한다. | 날짜/금액/수량/단가 파싱과 실패 안내가 정상이다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-17 | 매칭 결과와 의견을 저장·재조회한다. | 점수와 의견이 이용자/사업체에 잘 연결된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-18 | AI 도구(교육·평가·재작성 등)를 실행한다. | 로딩·성공·API 실패·재시도가 사용자에게 명확하다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-19 | 위기 문구를 입력한다. | 안전 안내가 우선 표시되고 부적절한 생성이 진행되지 않는다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-20 | 정보/복지 외부 링크를 연다. | 링크가 의도된 기본 브라우저에서 열리고 앱이 안전하게 유지된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-26 | 연결 지출이 있는 세부 예산 항목 삭제를 취소/확인한다. | 취소 시 변경이 없고, 확인 후 저장 시 지출은 남으며 항목 연결만 해제된다. | PASS-WARN | 실제 화면에서 확인창 발생 확인. 승인 후 저장·재진입은 수동 검증 필요. |
| R-27 | 항목 연결 해제 후 집계·필터·CSV를 확인한다. | 사업 총 사용액에 포함되고 항목 미지정으로 집계·표시되며 고아 필터 ID가 없다. | PASS-WARN | 순수 helper에서 보존/해제/CSV 표시 통과. 화면 집계와 필터의 저장 후 재진입은 수동 검증 필요. |

### 5.4 설정·백업·복원

| ID | 절차 | 기대 결과 | 상태 | 증적 |
|---|---|---|---|---|
| R-21 | API 설정을 저장·수정·삭제한다. | 키가 화면/콘솔에 평문으로 노출되지 않고 잘못된 키를 안내한다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-22 | 전체 백업을 내보낸다. | 이용자/문서 데이터는 포함되지만 API 키는 제거된다. | PASS | 격리 IndexedDB 내보내기에서 중첩 API 키·토큰·비밀번호가 빈 값이며 일반 `tokenCount` 값은 유지됨을 확인. |
| R-23 | 백업을 별도 테스트 DB에 복원한다. | 복원 전 데이터 보호·형식 검증·복원 후 조회가 정상이다. | PASS | 별도 origin에서 유효 백업 복원, 현재 인증정보 보존, 비인증 설정값 복원을 확인. |
| R-24 | 빈 DB/손상된 JSON/구버전 데이터를 복원한다. | 앱이 중단되지 않고 실패 원인과 복구 방법을 안내한다. | PASS | 빈 객체·배열이 아닌 스토어를 사전 거부하고 quota 장애 주입 시 스냅샷 롤백 및 복구 안내를 확인. |
| R-25 | 설정 초기화 또는 이용자 삭제를 실행한다. | 확인 단계가 있고 대상 범위가 정확하며 다른 데이터가 삭제되지 않는다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| R-28 | 직업평가 이력이 있는 전체 백업을 내보내고 복원한다. | `vocationalEvaluationHistory`가 허용 필드만 포함해 내보내지고 복원되며 현재 API 키는 유지된다. | PASS | 격리 origin에서 이력 내보내기·복원 및 주입 `apiKey` 필드 제외 확인. |
| R-29 | 직업평가 이력 필드가 없는 구버전 백업을 복원한다. | 복원은 성공하고 현재 직업평가 이력을 삭제하지 않는다. | PASS | 격리 origin에서 기존 이력 ID가 그대로 유지됨을 확인. |
| R-30 | 손상된 직업평가 이력이 포함된 백업을 복원한다. | 변경 전에 거부되고 현재 IndexedDB·예산·직업평가 이력·API 키가 보호된다. | PASS | 잘못된 이력 구조의 사전 거부와 현재 이력·인증정보 유지 확인. |

## 6. 데이터·보안·개인정보 점검

| ID | 확인 항목 | 기대 결과 | 상태 | 증적 |
|---|---|---|---|---|
| S-01 | IndexedDB 스토어와 DB 버전을 확인한다. | 스토어 누락·무한 업그레이드·차단 시 무한 대기가 없다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| S-02 | 민감 필드 저장값을 개발자 도구에서 확인한다. | 지정된 민감 필드가 암호화되어 저장되고 읽을 때만 복호화된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| S-03 | 복호화 키가 없거나 값이 손상된 상태를 확인한다. | 원본 데이터 손실 없이 오류를 안내하고 앱이 계속 동작한다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| S-04 | 백업 JSON을 검색한다. | API 키·토큰·비밀번호가 포함되지 않는다. | PASS | 기존 중첩 인증정보 제외와 함께 직업평가 이력은 허용 필드만 투영되어 주입한 `apiKey` 추가 필드가 내보내지지 않음을 확인. |
| S-05 | 네트워크 요청·콘솔·URL을 확인한다. | 실제 개인정보와 API 키가 불필요하게 전송/출력되지 않는다. | PASS-WARN | 정적 전수 검색으로 raw 로그와 API 오류 원문 전달을 제거. 실제 외부 API 요청은 실행하지 않아 네트워크 패널 확인 필요. |
| S-06 | HTML 입력/출력 필드를 확인한다. | 사용자 입력이 의도치 않게 HTML/스크립트로 실행되지 않는다. | PASS-WARN | 지출품의서 template의 title을 고정하고 사용자 제목은 `document.title` 속성으로 설정. React가 escape한 본문 DOM 복사 방식은 유지했으며 Electron 인쇄 실동작은 수동 검증 필요. |
| S-07 | 외부 링크와 새 창 동작을 확인한다. | 허용된 링크만 열리고 Electron 보안 경계가 유지된다. | PASS-WARN | 순수 URL helper의 HTTP/HTTPS 허용 및 file/javascript/data 차단 테스트 통과. Electron 실제 클릭은 미실행. |
| S-08 | 파일 업로드 크기·형식을 점검한다. | 과도한 파일·지원하지 않는 형식·악성 입력이 안전하게 거절된다. | PASS-WARN | 공통 검증으로 빈 파일·MIME/확장자·20/10/5MB 제한·중복을 FileReader/Base64 전에 거절. 실제 경계 파일 UI 업로드는 수동 검증 필요. |
| S-09 | 로그와 오류 메시지를 확인한다. | 진단에 필요한 정보만 남고 개인정보/비밀값은 마스킹된다. | PASS-WARN | 전역·React·DB·AI·OCR·화면 로그를 안전한 분류값으로 제한. 실제 API 오류 콘솔은 미실행. |

## 7. Electron·패키징 검증

| ID | 절차 | 기대 결과 | 상태 | 증적 |
|---|---|---|---|---|
| E-01 | `npm run electron:dev` 또는 동등한 실행을 수행한다. | 빌드 후 Electron 창이 열리고 `dist/index.html`을 로드한다. | PASS-WARN | Electron 36.9.5로 앱을 직접 실행해 JJSS 제목의 응답 가능 창과 프로세스를 확인. 창 내부 메뉴 순회는 자동화 권한 오류로 미실행. |
| E-02 | 창 최소 크기·포커스·메뉴바를 확인한다. | 설정값대로 표시되고 화면 깜빡임/포커스 손실이 없다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| E-03 | 외부 링크를 클릭한다. | 앱 내부가 아닌 기본 브라우저에서만 열리고 새 Electron 창은 차단된다. | PASS-WARN | URL helper 집중 테스트와 main 연결 확인. Electron 런타임 클릭은 수동 검증 필요. |
| E-04 | `npm run electron:build`를 실행한다. | 대상 OS/아키텍처 패키지가 생성되고 설치/실행된다. | PASS-WARN | 일반 build는 winCodeSign symlink 권한으로 실패. `signAndEditExecutable=false` unsigned 구조 검증은 NSIS 2.3.0 생성 성공. 정상 서명/리소스 편집 빌드는 Windows 개발자 모드 또는 symlink 권한이 필요하며 설치 실행은 미실행. |
| E-05 | 설치·업데이트·삭제를 확인한다. | 사용자 데이터 삭제 범위와 uninstall 설정이 의도와 일치한다. | PASS-WARN | 기존/신규 GUID 일치, per-machine 일치, `deleteAppDataOnUninstall: false` 확인. 실제 업그레이드·삭제는 데이터 보호를 위해 미실행. |
| E-06 | 패키징된 앱에서 백업·출력·파일 경로를 확인한다. | 개발 서버 전용 경로에 의존하지 않는다. | PASS-WARN | 기본 루트 선택과 전체 하위 폴더 생성을 자동 검증했다. 각 저장 대화상자 버튼은 설치형 앱에서 수동 검증 필요. |
| E-07 | 패키징된 앱에서 지출품의서를 인쇄한다. | 새 창 보안 정책에 차단되지 않고 로컬 인쇄 대화상자가 열린다. | PASS-WARN | `window.open` 제거와 hidden iframe 인쇄 구조·타입·빌드 확인. 패키징 Electron 실제 인쇄는 수동 검증 필요. |
| E-08 | signed build 환경변수 사전 검사를 실행한다. | 누락/없는 PFX는 패키징 전 실패하고 비밀값을 출력하지 않는다. | PASS | 누락 환경변수와 없는 로컬 경로 검사 통과. |
| E-09 | signed installer Authenticode를 확인한다. | 상태가 `Valid`이고 의도한 내부 인증서가 표시된다. | N/A | PFX 파일은 존재하지만 현재 셸에 `WIN_CSC_LINK`, `WIN_CSC_KEY_PASSWORD`가 없음. 미서명 구조 검증본은 `NotSigned`로 정확히 기록. |
| E-10 | 기존 설치 위 2.3.0 업그레이드를 실행한다. | 기존 설치를 감지하고 데이터와 userData 위치를 유지한다. | PASS-WARN | GUID/scope 정적·시스템 상태 검증은 통과. 실제 설치는 사용자 수동 검증 필요. |

## 8. 성능·접근성·호환성 점검

| ID | 확인 항목 | 기대 결과 | 상태 | 증적 |
|---|---|---|---|---|
| Q-01 | 초기 로드와 주요 페이지 전환을 측정한다. | 치명적인 장시간 블로킹·무한 로딩이 없다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| Q-02 | 긴 계획서/회의록/게시글을 입력한다. | 입력 지연·레이아웃 붕괴·출력 실패가 적절히 안내된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| Q-03 | 키보드만으로 주요 흐름을 수행한다. | 포커스 순서·모달 닫기·버튼 동작이 가능하다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| Q-04 | 화면 확대와 고대비 환경을 확인한다. | 텍스트가 잘리고 클릭 영역이 사라지지 않는다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| Q-05 | 최신 Chrome 2개 버전 범위에서 확인한다. | 핵심 흐름이 동일하게 동작한다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |
| Q-06 | Electron 패키지 환경에서 확인한다. | 브라우저 전용 API·파일 다운로드·인쇄 차이가 안내된다. | N/A | 이번 환경에서 해당 UI·API·패키징 흐름을 실제 실행하지 않아 수동 검증 필요. |

## 9. 정적 검토 시 발견 사항 분류

| ID | 심각도 | 파일:라인 | 문제/관찰 | 재현 절차 또는 근거 | 제안(이번 수정 여부) | 상태 |
|---|---|---|---|---|---|---|
| F-001 | P0 | `src/main.tsx:10`, `src/components/ErrorBoundary.tsx:26`, `src/store/dataStore.ts:150` 등 | raw 오류 객체가 API 응답·URL·이용자 입력을 콘솔/화면에 포함할 수 있음 | raw 로그 전수 검색 | `safeErrorMetadata`로 분류값만 기록하고 ErrorBoundary 원문 표시 제거 | 수정·타입검사 통과 |
| F-002 | P1 | `src/components/JobSeekerModal.tsx:151` | 일괄등록이 행별 저장 중 뒤 행 형식 오류로 부분 저장될 수 있음 | 순차 `await add*` 전에 전체 형식 검증이 없었음 | 모든 행 열 수·필수 이름을 먼저 검증하고 오류 행 안내 | 수정·정적 경로 확인 |
| F-003 | P1 | `src/services/gemini.ts:988` | OpenAI Bearer 값 끝 공백으로 인증 실패 가능 | Authorization template에 후행 공백 존재 | 공백 제거, 외부 API 오류 원문 대신 status 기반 일반 안내 | 수정·타입검사 통과 |
| F-004 | P1 | `src/components/ExpenseDocument.tsx:125` | 사용자 제목이 `document.write` template title에 직접 들어가 HTML로 해석될 수 있음 | 지출 제목에 HTML 특수문자 입력 | template title 고정 후 `document.title` 속성으로 설정 | 수정·빌드 통과 |
| F-005 | P2 | `src/components/Navbar.tsx:48`, `src/pages/Settings.tsx:114`, `src/pages/BudgetManagement.tsx:640` 등 | 다운로드 click 직후 ObjectURL 해제로 Electron/Chromium 저장 race 가능 | Blob 다운로드 경로 전수 검색 | 1.5초 뒤 해제, Minutes 누락 해제 추가 | 수정·타입검사 통과 |
| F-006 | P1 | `src/utils/fileValidation.ts:17`, `src/services/ocr.ts:136` 등 | 큰/빈/오형식 파일을 Base64로 읽어 메모리·API 실패 유발 | FileReader/Base64 경로 전수 검색 | 기능별 20/10/5MB, MIME/확장자, 빈 파일, 중복 검증 | 수정·빌드 통과 |
| F-007 | P1 | `src/pages/InfoMate.tsx:29` | 저장된 사용자 URL이 비HTTP 프로토콜일 수 있음 | legacy 및 신규 링크 렌더/저장 경로 확인 | 저장·렌더 모두 http/https 파싱 허용 | 수정·타입검사 통과 |
| F-008 | P2 | `src/pages/WorkMate.tsx:106`, `src/components/MatchingView.tsx:24`, `src/components/Toast.tsx:17` | 미리보기 URL·지연 타이머가 언마운트 후 남을 수 있음 | ObjectURL/setTimeout cleanup 검색 | URL 삭제·언마운트 해제, 매칭/Toast timer cleanup | 수정·타입검사 통과 |
| F-009 | P2 | `docs/WINDOWS_INTERNAL_SIGNING.md:7` | 문서 PFX 예시가 실제 파일 경로와 불일치 | 실제 파일 존재 경로와 문서 대조 | `%USERPROFILE%\Documents\JJSS-CodeSigning.pfx`로 문서만 수정 | 수정·경로 확인 |
| F-010 | P2 | `vite.config.ts:7` | 이전 개발자 PC 절대 경로가 Vite 설정에 남아 샌드박스 빌드에서 상위 경로 접근 오류를 유발함 | `npm run build`에서 config resolve 실패 재현 | 불필요한 `server.fs.allow` 하드코딩만 제거 | 수정·샌드박스 밖 빌드 통과 |
| F-011 | P1 | `package.json`, `electron/jjss-installer.nsh` | 기존 설치 실행파일은 `JJSS-Pro.exe`인데 신규 패키지 실행파일명이 달라 업그레이드 검사·제거 호환성이 불명확함 | 설치 레지스트리·Program Files 실측 | 실행파일명을 `JJSS-Pro`로 맞추고 강제종료 없는 exact-name 검사 연결 | 수정·NSIS 빌드 통과, 실제 업그레이드 승인 대기 |
| F-012 | P1 | 다운로드 기능 전반 | Electron renderer 다운로드만으로는 고정된 JJSS 문서 경로와 저장 완료 위치를 보장하지 못함 | 다운로드 API 전수 검색 | 제한된 preload/IPC 저장 서비스와 Browser fallback 도입 | 수정·타입/빌드/합성 가져오기 테스트 통과 |

### 심각도 기준

- `P0`: 데이터 유실, 개인정보/키 유출, 앱 실행 불가
- `P1`: 핵심 업무 흐름 중단, 잘못된 저장·계산·출력
- `P2`: 특정 조건에서 기능 오류, 회귀, 명확한 사용성 문제
- `P3`: 비차단 경고, 문서/정리·장기 개선 사항

## 10. 정적·런타임 결과 요약

| 영역 | PASS | PASS-WARN | FAIL | N/A | 메모 |
|---|---:|---:|---:|---:|---|
| 전체 파일 인벤토리 | 26 | 87 | 0 | 0 | 신규 릴리즈/매핑 스크립트·체크리스트·안전 DOCX 템플릿·출력 유틸까지 정적 검토. TODO 0건. |
| 타입/빌드 | 1 | 1 | 0 | 0 | TypeScript PASS, Vite는 기존 비차단 경고만 있음. |
| 라우팅/화면 | 8 | 4 | 0 | 0 | 계획서 PNG 4종과 DOCX 구조 생성 통과. PDF/DOCX 실제 버튼·Word 페이지·예산 저장 재진입은 수동 검증 필요. |
| 데이터 저장/복원 | 7 | 0 | 0 | 0 | 기존 검증과 직업평가 이력 내보내기/복원·구버전 보존·손상값 사전 거부를 격리 환경에서 확인. |
| 보안/개인정보 | 2 | 2 | 0 | 0 | 백업 인증정보·직업평가 이력 추가 필드 제외와 URL helper 테스트 통과. 실제 Electron/API 오류 로그 검증 필요. |
| AI/OCR/외부 연동 | 0 | 1 | 0 | 0 | 모델/오류 분류 정적 확인, 실제 외부 API 미호출. |
| Electron/패키징 | 1 | 5 | 0 | 1 | Electron 36.9.5 창 생성·응답 확인, NSIS 2.3.0 생성, 기존 GUID/scope 일치. PFX는 있으나 서명 환경변수 미설정으로 signed build N/A, 실제 업그레이드 미실행. |
| 접근성/성능/호환성 | 0 | 0 | 0 | 1 | 브라우저 기본 렌더링만 확인. 체계적 접근성·성능·다중 브라우저 검증은 이번 최소 안정화 범위 밖. |

## 11. 이번 작업 범위 밖의 제안

아래 항목은 전체 검증 중 발견하더라도 이번 작업에서 직접 수정하지 않고 제안으로만 기록합니다.

- 결재란 전자서명 및 결재 이력
- 양식별 템플릿 버전·권한 관리
- 출력 전 비식별화 모드
- PDF 반복 머리글·프린터별 고급 페이지 나눔
- 페이지별 PNG 및 ZIP 묶음 출력
- 장기적인 lazy loading 및 번들 분할
- 자동화 테스트 러너·CI 파이프라인·커버리지 수집

상세 제안은 [`docs/2026-08-12-future-improvement-proposals.md`](docs/2026-08-12-future-improvement-proposals.md)를 참고합니다.

## 12. 최종 승인

### 필수 통과 조건

- [x] 인벤토리의 모든 파일이 `PASS`, `PASS-WARN`, `FAIL`, `N/A` 중 하나로 판정됨
- [x] 타입 검사와 프로덕션 빌드 결과가 기록됨
- [ ] 핵심 흐름(R-01~R-13)이 통과함
- [x] 데이터·보안 점검(S-01~S-09)에 미해결 P0/P1 문제가 없음
- [x] Electron 실행/패키징의 가능한 범위가 수행되고 미실행 항목이 구분됨
- [x] `FAIL` 0건이며 발견 이슈의 원인·수정·검증을 기록함

| 항목 | 결과/의견 |
|---|---|
| 필수 조건 충족 여부 | 조건부 충족. 정적·빌드·Electron 실행/패키징 범위는 충족, 실제 외부 API·업그레이드·signed 설치는 미실행 |
| 미해결 P0/P1 | 확인된 항목 없음 |
| 조건부 승인 사유 | 실제 기존 설치 위 업그레이드, signed Authenticode, Electron 메뉴/인쇄/PDF 전체 UI 흐름은 사용자 PC 수동 검증 필요 |
| 최종 승인자 | 사용자 확인 필요 |
| 승인 일시 | 2026-08-14 검토 완료, 배포 승인 대기 |
| 후속 작업 문서/티켓 | `docs/2026-08-12-future-improvement-proposals.md` |

최종 판정은 단순히 빌드가 성공했다는 이유만으로 `통과` 처리하지 않습니다. 전체 파일 인벤토리와 핵심 런타임·보안·데이터 검증 결과를 함께 근거로 남깁니다.

## 13. 2026-08-17 AI 설정 추가 검증

| ID | 확인 항목 | 기대 결과 | 상태 | 증적 |
|---|---|---|---|---|
| AI-01 | 신규 모델 목록과 기본값 | Gemini 2개/OpenAI 3개/Claude 3개만 노출되고 기본값은 3.6 Flash/Terra/Sonnet 5다. | PASS | 중앙 모델 정의와 `npm run test:ai-settings` 확인 |
| AI-02 | 과거 모델 마이그레이션 | 지정된 구 모델은 신규 대응 모델로, 지원하지 않는 값은 제공사 기본값으로 보정된다. | PASS | Gemini/OpenAI/Claude 합성 migration fixture 전체 통과 |
| AI-03 | 기존 API 키 보호 | 모델 보정 시 기존 암호문과 복호화된 키, Vision 키가 초기화되지 않는다. | PASS-WARN | `encryptSettings(..., true)` 보존 경로 정적 확인. 실제 기존 설치 UI 확인 필요 |
| AI-04 | API 키 0개 최초 실행 | 키와 onboarding flag가 모두 없을 때만 안내가 표시된다. | PASS | 순수 조건 테스트와 빈 저장소 로컬 브라우저 렌더링 통과 |
| AI-05 | 나중에 설정 | localStorage flag를 남기고 비AI 기능을 차단하지 않으며 반복 표시하지 않는다. | PASS | 실제 클릭 후 모달 종료 및 새로고침 후 미재표시 확인 |
| AI-06 | onboarding 키 저장 | 하나 이상의 키를 기존 암호화 settings 흐름으로 저장하고 자동 API 요청은 하지 않는다. | PASS-WARN | 저장 handler에 네트워크 호출 없음. 실제 키 저장은 미실행 |
| AI-07 | 키 누락 안내 | AI 실행 시 필요한 provider를 안내하고 설정 화면 이동 버튼을 제공한다. | PASS | 키 없는 공문서 AI 실행에서 Gemini 안내와 설정 화면 이동 확인 |
| AI-08 | API endpoint 호환 | Gemini generateContent, OpenAI Chat Completions, Anthropic Messages 구조를 유지한다. | PASS-WARN | 공식 지원 문서 및 정적 호출 확인. 실제 유료 API 미호출 |
| AI-09 | 모델 접근/한도 오류 | 403/404, 429, Gemini 503을 구분하고 자동 유료 모델 전환을 하지 않는다. | PASS-WARN | 상태 분기 정적 확인. 실제 provider 오류 미주입 |

- 데이터 구조 영향: `DB_VERSION` 3 유지, IndexedDB store 추가/변경 없음, 기존 사용자 업무 데이터 구조 변경 없음.
- 보안 영향: API 키 암호화/복호화 방식 유지, 백업 제외 정책 유지, 키 로그·URL 전달 없음.
- 남은 수동 검증: 신규 설치 UI, 기존 암호화 키가 있는 업데이트 설치, 실제 provider 계정별 모델 접근 권한과 오류 응답.

## 14. 2026-08-17 AI 자동전환·유료 사용 정책 검증

| ID | 확인 항목 | 기대 결과 | 상태 | 증적 |
|---|---|---|---|---|
| AF-01 | 안전 기본값 | 자동전환 OFF, economy, premium 자동승급 OFF, 확인 ON | PASS | `DEFAULT_AI_FAILOVER`, 합성 테스트 |
| AF-02 | 구버전 설정 보정 | 누락 필드를 안전 기본값으로 채우고 API 키·선택 모델을 유지 | PASS | `normalizeAIFailover`, settings 병합 경로 |
| AF-03 | economy/balanced premium 금지 | 어떤 허용 오류에도 premium 자동 요청 없음 | PASS | 계획 생성 및 executor 실패 주입 테스트 |
| AF-04 | premium 명시 동의 | 최초 premium 정책 선택 확인 후에만 저장 | PASS | 로컬 UI에서 취소 시 economy 유지, 승인 후 premium 보호 옵션 노출 확인 |
| AF-05 | premium 자동승급·확인 | 자동승급 ON에서만 premium 후보 생성, 확인 ON이면 호출 전 결정 | PASS | executor 합성 테스트 |
| AF-06 | premium 취소 | 취소 후 premium API callback 미호출 | PASS | 호출 목록이 balanced 1회뿐임을 확인 |
| AF-07 | cross-provider 독립 동의 | premium 동의만으로 타사 후보가 생성되지 않음 | PASS | consent plan 테스트와 premium 승인 후 타사 동의 취소 UI 확인 |
| AF-08 | credit 부족 처리 | 같은 Provider를 다시 호출하지 않고 동의된 다음 Provider만 검토 | PASS | creditUnavailable 실패 주입 테스트 |
| AF-09 | 최대 시도 상한 | 한 작업 최대 3회, 무한 loop 없음 | PASS | `MAX_AI_REQUEST_ATTEMPTS = 3`, 호출 수 assertion |
| AF-10 | 비용 표시 | 비용 가능성만 알리고 실제 금액은 계산하지 않음 | PASS | 금액 계산/usage metadata 미구현, 안내 문구 정적 확인 |

- 실제 유료 API, 결제, 충전, Billing, 구독, 크레딧 구매 요청은 실행하거나 구현하지 않았다.
- 실제 Provider 장애를 이용한 네트워크 failover는 비용·개인정보 전송을 피하기 위해 실행하지 않고 합성 callback으로 검증했다.
## 15. AI API 과다·중복 호출 방지 검증 (2026-08-17)

- 공통 안전 모듈: `src/services/aiRequestSafety.ts`
- Failover 단일 실행 계층: `src/services/aiFailoverExecutor.ts`
- 호출 지도: `docs/AI_API_CALL_MAP.md`
- 자동 검증: `npm run test:ai-safety`
- 검증 범위: 더블클릭, in-flight lock, 자동 retry 0회, 최대 3회, Provider 재방문 금지, 취소 후 failover 금지, useEffect/파일 선택/catch 재호출 금지, 로컬 저장·출력 no-AI
- 테스트는 가짜 Provider만 사용하며 실제 API 키 또는 네트워크를 사용하지 않습니다.

## 16. API 오류·첨부파일·Windows 설치·기존 문서 최종 안정화 (2026-08-17)

| ID | 확인 항목 | 상태 | 증적 |
|---|---|---|---|
| FS-01 | 일반 429와 명시적 quota 분리 | PASS | 일반 429/`RESOURCE_EXHAUSTED`는 `rateLimit`, `QUOTA_EXCEEDED`/일일 quota 소진 문구는 `quotaExceeded`; 합성 호출 목록 확인 |
| FS-02 | 401·403·404 분리 | PASS | 401 `authError`, 403 `permissionError`, 실제 `MODEL_NOT_FOUND` 404만 `modelUnavailable`; 일반 404는 전환하지 않음 |
| FS-03 | 안전한 기본 자동전환 | PASS | `rateLimit=false`, `permissionError=false`; 일반 429와 403은 Provider 1회 후 종료, 명시적 quota/모델 부재만 기존 동의 조건에서 전환 |
| FS-04 | 첨부파일 Provider lock | PASS | 현재 첨부 지원 Provider는 Gemini만 명시. 첨부 503에서 Gemini 1회만 호출하고 OpenAI/Claude prompt-only 전환 없음 |
| FS-05 | 텍스트 failover·안전상한 회귀 | PASS | retry 0, Provider당 1회, Provider 재방문 없음, Job당 최대 3회, double-click 차단 테스트 유지 |
| FS-06 | 하위 폴더 가져오기 | PASS | 기본 ON, 깊이 5, 최대 5,000개, junction/symlink 제외; 합성 트리 9/9 및 일반 파일 보호·SHA-256 이동 검증 통과 |
| FS-07 | `customCheckAppRunning` 연결 | PASS | electron-builder 25.1.8 `installSection.nsh`가 `CHECK_APP_RUNNING`을 삽입하고 사용자 매크로가 정의된 경우 이를 호출함. unsigned NSIS 구조 빌드 컴파일 통과 |
| FS-08 | Windows cannot-be-closed | PASS-WARN | 영문 문구는 `extractAppPackage.nsh`의 `CopyFiles` 실패 경로임을 확인. 미실행 상태 프로세스·서비스·시작 항목 0건, 설치 폴더 ACL은 일반 사용자 읽기/실행·관리자 쓰기. 실제 기존 설치 위 upgrade는 미실행 |

### Windows 설치 실측

- electron-builder: `25.1.8`
- 기존 설치: HKLM, `/allusers`, `C:\Program Files\JJSS-Pro`, 버전 1.0.0
- 기존/신규 실행파일: `JJSS-Pro.exe`
- appId: `com.jjss.desktop`; `perMachine: true` 유지
- 실행중 검사: `${APP_EXECUTABLE_FILENAME}` exact-name 한 개만 검사. 오래된 `JJSS.exe` 오탐 후보 제거. `taskkill`/강제 종료 없음
- 영문 오류 위치: electron-builder `templates/nsis/include/extractAppPackage.nsh`의 파일 복사 5회 실패 후 안내. 사용자 정의 실행중 안내는 한국어이므로 서로 다른 경로임
- 검증 패키징: 명령행에서 서명/리소스 편집을 끈 구조 검증용 NSIS 2.3.0 생성 성공 후 임시 산출물 삭제. 배포용 서명 설치파일 판정이 아님
- Fresh install / 기존 설치 위 upgrade / 동일 installer 재실행 / 앱 실행 상태 install: 실제 시스템 변경을 수행하지 않아 모두 수동 검증 필요. 따라서 설치 문제는 `PASS`가 아닌 `PASS-WARN`

### 변경 금지 항목 확인

- `DB_VERSION`, IndexedDB store, appId, userData/Documents 구조, API 키 암호화, 라우트·Navbar를 변경하지 않음
- SDK retry 1 attempt, router retry 0, Job 최대 3회, Provider당 1회를 유지
- AppData·레지스트리 삭제, `taskkill`, PC 전체 문서 자동검색을 추가하지 않음

## 17. 최종 배포 전 정리 (2026-08-17)

| 확인 항목 | 상태 | 결과 |
|---|---|---|
| normalized reason 정합성 | PASS | 사용자 메시지·분류·전환 조건 변경 없이 `rateLimit→rate-limit`, `creditUnavailable→credit` metadata만 수정 |
| API 비용 안전 | PASS | automatic retry 0, Provider당 1회, Job당 3회, 재방문 없음, attachment lock 유지 |
| scripts Git 포함 | PASS | package.json 참조 6개 파일이 모두 `git ls-files scripts`에 존재하고 staged source 대상임 |
| `.gitignore` | PASS | release/dist/node_modules/.env*/PFX·개인키/.DS_Store/.temp_app_extract 제외 확인 |
| signed build | N/A | 현재 shell에 `WIN_CSC_LINK`, `WIN_CSC_KEY_PASSWORD` 없음. 실제 빌드·Authenticode 검증 미실행 |
| 실제 upgrade | PASS-WARN | 기존 설치 제거 없이 수행할 실환경 upgrade와 cannot-be-closed 회귀 검증이 남아 있음 |

## 18. 입력·문서 저장 런타임 안정화 (2026-08-17)

- 현재 package 및 lock root version: `3.0.0` 일치.
- 예산 폼 입력은 functional updater를 사용해 batched update에서 다른 필드가 사라지지 않도록 보완.
- 업무지원 tool form 초기화는 `activeTool?.id`가 실제 변경될 때만 수행.
- PDF/PNG/DOCX는 기본 JJSS 폴더를 제안하되 사용자가 선택한 일반 경로를 허용하고, 실행 중 최근 저장 폴더를 다음 대화상자에 제안함. 취소는 오류로 표시하지 않음.
- PNG 단계 추적과 PNG/PDF signature 검증은 자동 테스트 통과. 실제 Windows Electron 저장은 PASS-WARN.
- `public/icon.png`가 없어 macOS package icon asset은 PASS-WARN이며 이번 Windows 안정화 작업에서 임의 생성하지 않음.
